# Fixed admin credentials (change for production)
admin.username=admin
admin.password=admin123

# Thymeleaf cache disabled for fast template iteration in dev (optional)
spring.thymeleaf.cache=false

# --- Error handling & server settings ---
server.error.whitelabel.enabled=false
server.error.include-stacktrace=never
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=true
spring.mvc.view.encoding=UTF-8

# --- H2 Database Configuration ---
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (for development)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# WhatsApp Business Configuration
whatsapp.business.number=+919876543210
whatsapp.business.name=Customised Gifts Store
